services:
  db:
    image: postgres:16
    restart: unless-stopped
    environment:
      POSTGRES_DB: adaremise
      POSTGRES_USER: adaremise_user
      POSTGRES_PASSWORD: motdepasse123
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata: